//
//  PurchasetAddPricePromotionsModel.m
//  sosoYY
//
//  Created by zhy on 17/3/7.
//  Copyright © 2017年 felix. All rights reserved.
//

#import "PurchasetAddPricePromotionsModel.h"

@implementation PurchasetAddPricePromotionsModel

-(id)copyWithZone:(NSZone *)zone {

    
    PurchasetAddPricePromotionsModel * liModel = [[[self class] allocWithZone:zone] init];
    // 加价购说明
    liModel.name = self.name;
    // 加价购规则编号
    liModel.pmid = self.pmid;
    // 加多少钱
    liModel.addPrice = self.addPrice;
    // 加价规则类型，0：满X，1：每满X
    liModel.addPriceType = self.addPriceType;
    // 主商品开始数
    liModel.firstProudctStartNum = self.firstProudctStartNum;
    // 主商品每次数
    liModel.firstProudctPerNum = self.firstProudctPerNum;
    // 次商品数
    liModel.secondProudctNum = self.secondProudctNum;
    // 加价购商品编号
    liModel.pid = self.pid;
    // 通用名
    liModel.DrugsBase_DrugName = self.DrugsBase_DrugName;
    // 商品名
    liModel.DrugsBase_ProName = self.DrugsBase_ProName;
    // 规格
    liModel.DrugsBase_Specification = self.DrugsBase_Specification;
    liModel.DrugsBase_Manufacturer = self.DrugsBase_Manufacturer;
    liModel.Stock = self.Stock;
    // 效期
    liModel.sxrq = self.sxrq;
    liModel.Goods_Unit = self.Goods_Unit;
    
    liModel.IsJxq = self.IsJxq;
    return liModel;
}

-(id)mutableCopyWithZone:(NSZone *)zone {
    
    
    PurchasetAddPricePromotionsModel * liModel = [[[self class] allocWithZone:zone] init];
    // 加价购说明
    liModel.name = self.name;
    // 加价购规则编号
    liModel.pmid = self.pmid;
    // 加多少钱
    liModel.addPrice = self.addPrice;
    // 加价规则类型，0：满X，1：每满X
    liModel.addPriceType = self.addPriceType;
    // 主商品开始数
    liModel.firstProudctStartNum = self.firstProudctStartNum;
    // 主商品每次数
    liModel.firstProudctPerNum = self.firstProudctPerNum;
    // 次商品数
    liModel.secondProudctNum = self.secondProudctNum;
    // 加价购商品编号
    liModel.pid = self.pid;
    // 通用名
    liModel.DrugsBase_DrugName = self.DrugsBase_DrugName;
    // 商品名
    liModel.DrugsBase_ProName = self.DrugsBase_ProName;
    // 规格
    liModel.DrugsBase_Specification = self.DrugsBase_Specification;
    liModel.DrugsBase_Manufacturer = self.DrugsBase_Manufacturer;
    liModel.Stock = self.Stock;
    // 效期
    liModel.sxrq = self.sxrq;
    liModel.Goods_Unit = self.Goods_Unit;
    
    liModel.IsJxq = self.IsJxq;
    return liModel;
}

@end
